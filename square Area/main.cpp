
#include<iostream>
using namespace std;

int main()
{
    int length = 0, area = 0;
    cout << "Enter the length of side of a square: " << flush;
    cin >> length;
    area = length * length;
    cout << " Area of square is : " << area << endl;
    return 0;
}
